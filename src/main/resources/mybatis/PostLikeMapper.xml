<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.learning.healthpro.mapper.PostLikeMapper">
    <insert id="insertLike">
        insert into post_like(user_id, post_id, date) values(#{userId}, #{postId}, #{date})
    </insert>

    <delete id="deleteLike">
        delete from post_like where user_id = #{userId} and post_id = #{postId}
    </delete>

    <select id="existsLike" resultType="boolean">
        select count(*) > 0 from post_like where user_id = #{userId} and post_id = #{postId}
    </select>
</mapper>
