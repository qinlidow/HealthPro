<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.learning.healthpro.mapper.CommentMapper">
    <select id="getCommentsByPostId" resultType="com.learning.healthpro.entity.Comment">
        select id, post_id, user_id, user_name, content, date from comment where post_id = #{postId} order by id asc
    </select>

    <insert id="insertComment" useGeneratedKeys="true" keyProperty="id">
        insert into comment(post_id, user_id, user_name, content, date)
        values(#{post_id}, #{user_id}, #{user_name}, #{content}, #{date})
    </insert>

    <delete id="deleteCommentById">
        delete from comment where id = #{id}
    </delete>
</mapper>
