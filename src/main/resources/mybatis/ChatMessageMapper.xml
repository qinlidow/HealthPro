<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.learning.healthpro.mapper.ChatMessageMapper">

    <select id="getByUserId" resultType="ChatMessage">
        SELECT * FROM chat_message WHERE user_id = #{userId} ORDER BY id ASC
    </select>

    <insert id="add" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_message (role, content, date, user_id)
        VALUES (#{role}, #{content}, #{date}, #{user_id})
    </insert>

    <delete id="deleteByUserId">
        DELETE FROM chat_message WHERE user_id = #{userId}
    </delete>

</mapper>
